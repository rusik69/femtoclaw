services:
  femtoclaw:
    build:
      context: .
      dockerfile: Containerfile
    image: femtoclaw:local
    working_dir: /work
    volumes:
      - ./work:/work
    env_file:
      - .env
    restart: unless-stopped
